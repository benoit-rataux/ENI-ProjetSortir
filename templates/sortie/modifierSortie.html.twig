{% extends 'sortie/creerSortie.html.twig' %}

{% block title %}{{ sortie.nom }}{% endblock %}

{% block body %}
    {{ parent() }}
    
    <a class="bg-gray-400 hover:bg-red-300 text-white font-bold py-2 px-4 mt-1.5 w-40 rounded"
       href="{{ path('app_sortie_supprimer',{'id' : sortie.id}) }}">Supprimer!</a>
{% endblock %}

